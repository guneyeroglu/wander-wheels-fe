import { FC } from 'react';

import { mockData } from '../../global/constants';

const CarList: FC = () => {
  console.log(mockData);

  return (
    <div>
      <div>CarList</div>
    </div>
  );
};

export default CarList;
